<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/background">


    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/ic_logo"
        android:contentDescription="Locket Logo"
        android:layout_marginBottom="24dp"/>


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Locket"
        android:fontFamily="@font/craftroundedbold"
        android:textColor="@color/white"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>


    <TextView
        android:layout_width="248dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="26dp"
        android:layout_marginBottom="32dp"
        android:fontFamily="@font/craftboundedregular"
        android:gravity="center"
        android:text="Ảnh trực tiếp từ bạn bè, ngay trên màn hình chính"
        android:textColor="@color/gray"
        android:textSize="22sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btn_register"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:text="Tạo một tài khoản"
        android:textSize="16sp"
        android:fontFamily="@font/comfortaa"
        android:backgroundTint="@color/button"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"
        android:elevation="28dp"
        android:paddingBottom="12dp"
        android:paddingTop="12dp"
        />

    <Button
        android:id="@+id/btn_login"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:text="Đăng nhập"
        android:textSize="16sp"
        android:backgroundTint="@color/background"
        android:textColor="@color/white"/>
</LinearLayout>
